var id = 1
const initialState = {

    movies: [{ "name": "volver al futuro", "año": 1988 }],

}
export const reducer = (state = initialState, action) => {
    switch (action.type) {

        default:
            return state
    }
}

